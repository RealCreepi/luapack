# luapack
## Usage
```
usage: luapack.py [-h] -t TARGET [--ignore-unknown-pkg]

Packages your Lua project into a single .lua file

options:
  -h, --help            show this help message and exit
  -t TARGET, --target TARGET
                        .lua file which will be packaged
  --ignore-unknown-pkg  Ignores unknown packages
```
